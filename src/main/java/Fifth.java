public class Fifth {
    static String text = "Local lol";
    static int  a = 5;
    public String concatenation(){
        class Local{
            public double powpow(){
                int c =10;
                double b =  Math.pow((double)c, 10.0);
                return b;
            }

        }
        Local local = new Local();
        return a+ local.powpow() +" "+ text;
    }
}
